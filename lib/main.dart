import 'package:flutter/material.dart';

void main() {
  runApp(MaterialApp(
    home: Scaffold(
      appBar: AppBar(title: Text('Runner Game Placeholder')),
      body: Center(child: Text('Runner Game is working!')),
    ),
  ));
}
